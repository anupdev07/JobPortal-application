<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Manage Jobs for <%= company.companyName %></title>
    <style>
        /* Main container */
        .container {
            display: flex;
            min-height: 100vh;
        }

        /* Sidebar Navigation */
        .sidebar {
            background: #2a2a40;
            width: 250px;
            padding: 1rem;
            display: flex;
            flex-direction: column;
        }
        .sidebar a {
            color: #fff;
            text-decoration: none;
            padding: 1rem;
            font-size: 1.1rem;
            margin-bottom: 0.5rem;
            transition: background 0.3s ease;
        }
        .sidebar a:hover {
            background: #404068;
        }

        /* Content Area */
        .content {
            flex-grow: 1;
            padding: 2rem;
            background: #f4f4f9;
        }
    </style>
</head>
<body>
    <div class="container">
        <!-- Sidebar Navigation -->
        <div class="sidebar">
            <h2>Job Dashboard</h2>
            <a href="/company/<%= company.companyId %>/jobs/create">Create New Job</a>
            <a href="/company/<%= company.companyId %>/jobs/view">Your Job Listings</a>
            <a href="/company/<%= company.companyId %>/jobs/applications">View/Manage Applications</a>
        </div>

        <!-- Main Content Area -->
        <div class="content">
            <h1>Manage Jobs for <%= company.companyName %></h1>
            <p>Select an option from the sidebar to manage jobs for this company.</p>
        </div>
    </div>
</body>
</html>
