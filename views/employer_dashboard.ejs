<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Employer Dashboard</title>
  <style>
    /* Base styling */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    body, html {
      height: 100%;
      font-family: Arial, sans-serif;
      background: linear-gradient(135deg, #1a1a2e, #16213e);
      color: #e0e0e0;
      display: flex;
    }

    /* Sidebar */
    .sidebar {
      width: 250px;
      background: #111;
      padding: 2rem 1rem;
      display: flex;
      flex-direction: column;
      align-items: center;
      height: 100vh;
    }
    .sidebar h2 {
      color: #c9a6ff;
      margin-bottom: 2rem;
      font-size: 1.8rem;
      text-align: center;
    }
    .sidebar a {
      color: #eaeaf0;
      font-size: 1.1rem;
      margin: 1rem 0;
      text-decoration: none;
      transition: color 0.3s ease;
    }
    .sidebar a:hover {
      color: #7f59ff;
    }
    .logout {
      margin-top: auto;
      color: #fff;
      background-color: #e63946;
      padding: 0.5rem 1rem;
      font-size: 1rem;
      font-weight: bold;
      border: none;
      border-radius: 5px;
      cursor: pointer;
      text-decoration: none;
      transition: background 0.3s ease;
    }
    .logout:hover {
      background-color: #c52834;
    }

    /* Main content */
    .main-content {
      flex: 1;
      padding: 2rem;
      display: flex;
      flex-direction: column;
      gap: 2rem;
      overflow-y: auto;
    }
    .main-content h1 {
      font-size: 2.5rem;
      color: #c9a6ff;
    }

    /* Dashboard Sections */
    .section {
      background: rgba(33, 31, 47, 0.85);
      border-radius: 12px;
      padding: 1.5rem;
      box-shadow: 0px 8px 16px rgba(127, 89, 255, 0.3);
    }
    .section h2 {
      font-size: 1.5rem;
      color: #d1c3ff;
      margin-bottom: 1rem;
    }
    .section p {
      font-size: 1rem;
      color: #b2b6c5;
    }

    /* Job List */
    .job-list {
      display: flex;
      flex-direction: column;
      gap: 1rem;
    }
    .job-item {
      background: #252a41;
      border-radius: 8px;
      padding: 1rem;
      display: flex;
      justify-content: space-between;
      align-items: center;
      color: #eaeaf0;
    }
    .job-item h3 {
      font-size: 1.2rem;
    }
    .job-item button {
      padding: 0.5rem 1rem;
      font-size: 1rem;
      color: #fff;
      background: linear-gradient(135deg, #7f59ff, #a368d8);
      border: none;
      border-radius: 5px;
      cursor: pointer;
      transition: all 0.3s ease;
    }
    .job-item button:hover {
      transform: scale(1.05);
    }

    /* Add New Job Button */
    .add-job {
      padding: 1rem;
      font-size: 1.1rem;
      color: #fff;
      background: linear-gradient(135deg, #6a52d8, #5e4aca);
      border: none;
      border-radius: 5px;
      cursor: pointer;
      transition: background 0.3s ease;
      margin-top: 1rem;
      align-self: start;
    }
    .add-job:hover {
      background: linear-gradient(135deg, #5e4aca, #4b3ba9);
    }
  </style>
</head>
<body>

  <!-- Sidebar -->
  <div class="sidebar">
    <h2>Employer Dashboard</h2>
    <a href="#job-postings">Job Postings</a>
    <a href="#applicants">Applicants</a>
    <a href="#company-profile">Company Profile</a>
    <a href="#logout" class="logout">Logout</a>
  </div>

  <!-- Main Content -->
  <div class="main-content">
    
    <!-- Header -->
    <h1>Welcome to Your Employer Dashboard</h1>

    <!-- Job Postings Section -->
    <div id="job-postings" class="section">
      <h2>Manage Job Postings</h2>
      <div class="job-list">
        <div class="job-item">
          <div>
            <h3>Frontend Developer</h3>
            <p>Kathmandu</p>
          </div>
          <button>Edit Posting</button>
        </div>
        <div class="job-item">
          <div>
            <h3>Backend Developer</h3>
            <p>Pokhara</p>
          </div>
          <button>Edit Posting</button>
        </div>
        <!-- Additional job postings can be added here -->
      </div>
      <button class="add-job">+ Add New Job</button>
    </div>

    <!-- Applicants Section -->
    <div id="applicants" class="section">
      <h2>View Applicants</h2>
      <p>Review applicants who have applied for your job postings and manage the hiring process efficiently.</p>
      <!-- Example of applicant tracking can go here -->
    </div>

    <!-- Company Profile Section -->
    <div id="company-profile" class="section">
      <h2>Company Profile</h2>
      <p>Update your company details to attract the right talent and showcase your company’s strengths.</p>
      <button>Edit Profile</button>
    </div>
  </div>

</body>
</html>
